# Python dependencies for Lambda handlers
boto3>=1.34.0

# Optional: Optimized text similarity (faster than difflib)
# Uncomment if deploying with Lambda layers that support binary packages
# python-Levenshtein>=0.25.0
